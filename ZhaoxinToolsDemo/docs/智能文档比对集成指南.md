# 智能文档比对集成指南

本文档详细说明如何在你的项目中集成肇新智能文档比对功能。

## 📋 目录

- [功能概述](#功能概述)
- [集成方案](#集成方案)
- [快速开始](#快速开始)
- [后端集成](#后端集成)
- [前端集成](#前端集成)
- [完整示例](#完整示例)
- [常见问题](#常见问题)

---

## 功能概述

### 核心功能

- ✅ **PDF 文档比对** - 支持 PDF 格式的文档比对
- ✅ **智能去水印** - 可选的水印去除功能
- ✅ **差异高亮** - 自动标注文档差异
- ✅ **报告导出** - 支持 Word 和 HTML 格式导出
- ✅ **任务管理** - 比对任务的历史记录和管理

### 技术特点

- 🚀 **GPU 加速** - 使用 GPU 加速 OCR 识别
- 🎯 **高精准度** - 精准的差异识别算法
- 📊 **可视化结果** - 直观的差异对比展示
- 🔄 **异步处理** - 支持大文件的异步比对

---

## 集成方案

### 方案对比

| 方式 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| **iframe 嵌套** | 零代码，快速集成 | 样式难以自定义 | 快速原型，内部系统 |
| **API 对接** | 完全自定义 UI | 开发工作量大 | 品牌化需求强 |
| **混合方案** | 平衡定制与开发成本 | 需要理解两种方式 | **推荐方案** |

本 Demo 采用**混合方案**：
- 使用自定义 UI 实现文件上传和任务管理
- 使用 iframe 嵌套展示比对结果

---

## 快速开始

### 1. 环境准备

确保以下服务已启动：

| 服务 | 端口 | 说明 |
|------|------|------|
| 肇新后端 | 8080 | 核心 API 服务 |
| 肇新前端 | 3000 | 比对结果展示 |
| Demo 后端 | 8091 | 演示项目后端 |
| Demo 前端 | 3003 | 演示项目前端 |

### 2. 访问比对功能

启动 Demo 项目后，访问：

```
http://localhost:3003/compare
```

### 3. 第一次比对

1. 上传基准文件（旧版本 PDF）
2. 上传比对文件（新版本 PDF）
3. 可选：勾选"去除水印"
4. 点击"开始比对"
5. 等待比对完成
6. 查看或下载比对结果

---

## 后端集成

### 1. 项目结构

```
backend/
├── src/main/java/com/zhaoxin/tools/demo/
│   ├── controller/
│   │   └── CompareController.java         # 比对控制器
│   ├── service/
│   │   ├── ZhaoxinApiClient.java         # 肇新 API 客户端
│   │   └── TaskFileMappingService.java   # 文件名映射服务
│   ├── model/
│   │   ├── request/
│   │   │   └── CompareRequest.java       # 比对请求模型
│   │   ├── response/
│   │   │   └── ApiResponse.java          # 统一响应格式
│   │   └── TaskFileMapping.java          # 任务文件映射
│   └── config/
│       └── GlobalExceptionHandler.java   # 全局异常处理
```

### 2. 核心类说明

#### CompareController.java

代理比对相关的 API 请求，提供以下接口：

```java
@RestController
@RequestMapping("/api/compare")
public class CompareController {
    
    // 提交比对任务
    @PostMapping("/submit")
    public ApiResponse submitCompare(@RequestBody CompareRequest request)
    
    // 查询任务状态
    @GetMapping("/task/{taskId}")
    public ApiResponse getTaskStatus(@PathVariable String taskId)
    
    // 获取比对结果
    @GetMapping("/result/{taskId}")
    public ApiResponse getResult(@PathVariable String taskId)
    
    // 删除任务
    @DeleteMapping("/task/{taskId}")
    public ApiResponse<Void> deleteTask(@PathVariable String taskId)
    
    // 获取所有任务
    @GetMapping("/tasks")
    public ApiResponse getAllTasks()
    
    // 导出比对报告
    @PostMapping("/export-report")
    public ResponseEntity<byte[]> exportReport(@RequestBody Map<String, Object> exportData)
}
```

#### ZhaoxinApiClient.java

封装对肇新后端 API 的调用：

```java
@Service
public class ZhaoxinApiClient {
    
    @Value("${zhaoxin.api.base-url}")
    private String baseUrl;
    
    private final RestTemplate restTemplate;
    
    // 提交比对任务
    public ApiResponse submitCompareTask(String oldFileUrl, String newFileUrl, 
                                         Boolean removeWatermark,
                                         String oldFileName, String newFileName)
    
    // 查询任务状态
    public ApiResponse getCompareTaskStatus(String taskId)
    
    // 获取比对结果
    public ApiResponse getCompareResult(String taskId)
    
    // 删除任务
    public void deleteCompareTask(String taskId)
    
    // 获取所有任务
    public ApiResponse getAllCompareTasks()
    
    // 导出报告
    public ResponseEntity<byte[]> exportCompareReport(Map<String, Object> exportData)
}
```

### 3. 配置说明

**application.yml**

```yaml
server:
  port: 8091

zhaoxin:
  api:
    base-url: http://localhost:8080      # 肇新后端地址
    timeout: 60000

spring:
  servlet:
    multipart:
      max-file-size: 50MB                # 最大文件大小
      max-request-size: 50MB
```

### 4. 关键实现点

#### 4.1 文件名映射

由于肇新 API 返回的文件名可能是服务器生成的，我们需要保存用户上传的原始文件名：

```java
@Service
public class TaskFileMappingService {
    
    // 保存任务ID与原始文件名的映射
    public void saveMapping(String taskId, String oldFileName, String newFileName) {
        TaskFileMapping mapping = new TaskFileMapping(
            taskId, oldFileName, newFileName, System.currentTimeMillis()
        );
        mappingCache.put(taskId, mapping);
        persistMappings();  // 持久化到文件
    }
    
    // 获取映射
    public TaskFileMapping getMapping(String taskId) {
        return mappingCache.get(taskId);
    }
}
```

#### 4.2 统一异常处理

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(ApiException.class)
    @ResponseStatus(HttpStatus.BAD_REQUEST)
    public ApiResponse<Void> handleApiException(ApiException e) {
        log.error("API调用异常", e);
        return new ApiResponse<>(e.getCode(), e.getMessage(), null);
    }
}
```

---

## 前端集成

### 1. 项目结构

```
frontend/
├── src/
│   ├── views/
│   │   ├── Compare.vue              # 比对页面
│   │   └── CompareResult.vue        # 结果页面
│   ├── api/
│   │   └── compare.js               # 比对 API 客户端
│   ├── utils/
│   │   └── compareHelper.js         # 工具函数
│   └── config.js                    # 配置文件
```

### 2. 核心组件说明

#### Compare.vue - 比对页面

提供文件上传和比对功能：

```vue
<template>
  <div class="compare-page">
    <!-- 文件上传区域 -->
    <div class="upload-section">
      <!-- 旧文件上传 -->
      <div class="upload-zone" @click="selectOldFile">
        <!-- 文件选择界面 -->
      </div>
      
      <!-- 新文件上传 -->
      <div class="upload-zone" @click="selectNewFile">
        <!-- 文件选择界面 -->
      </div>
    </div>
    
    <!-- 操作区域 -->
    <div class="action-bar">
      <el-checkbox v-model="removeWatermark">去除水印</el-checkbox>
      <el-button @click="handleSubmit">开始比对</el-button>
    </div>
    
    <!-- 进度显示 -->
    <div v-if="comparing" class="progress-container">
      <el-progress :percentage="progress" />
      <p>{{ statusText }}</p>
    </div>
    
    <!-- 结果展示 -->
    <div v-if="success" class="result-section">
      <el-button @click="viewResult">查看详细结果</el-button>
      <el-button @click="downloadResult">下载比对报告</el-button>
    </div>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import compareApi from '@/api/compare'
import { pollTaskStatus } from '@/utils/compareHelper'

const oldFile = ref(null)
const newFile = ref(null)
const removeWatermark = ref(false)
const comparing = ref(false)
const progress = ref(0)
const currentTaskId = ref(null)

// 提交比对
const handleSubmit = async () => {
  // 1. 上传文件
  const oldFileUrl = await uploadFile(oldFile.value)
  const newFileUrl = await uploadFile(newFile.value)
  
  // 2. 提交比对任务
  const result = await compareApi.submitCompare(
    oldFileUrl, newFileUrl, removeWatermark.value,
    oldFile.value.name, newFile.value.name
  )
  currentTaskId.value = result.data
  
  // 3. 轮询任务状态
  await pollTaskStatus(currentTaskId.value, (prog, status) => {
    progress.value = prog
    statusText.value = status.statusMessage
  })
  
  // 4. 完成
  success.value = true
}
</script>
```

#### CompareResult.vue - 结果页面

使用 iframe 嵌套肇新前端的比对结果页：

```vue
<template>
  <iframe :src="iframeUrl" class="result-iframe" />
</template>

<script setup>
import { computed } from 'vue'
import { useRoute } from 'vue-router'
import { ZHAOXIN_CONFIG } from '@/config'

const route = useRoute()
const taskId = computed(() => route.params.taskId)

// iframe URL - 使用嵌入模式隐藏侧边栏
const iframeUrl = computed(() => 
  `${ZHAOXIN_CONFIG.frontendUrl}/gpu-ocr-compare/canvas-result/${taskId.value}?embed=true`
)
</script>

<style scoped>
.result-iframe {
  width: 100%;
  height: 100vh;
  border: none;
}
</style>
```

### 3. API 客户端

**compare.js**

```javascript
import axios from 'axios'

const request = axios.create({
  baseURL: '/api/compare',
  timeout: 60000
})

export default {
  // 提交比对任务
  submitCompare(oldFileUrl, newFileUrl, removeWatermark, oldFileName, newFileName) {
    return request.post('/submit', {
      oldFileUrl,
      newFileUrl,
      removeWatermark,
      oldFileName,
      newFileName
    })
  },
  
  // 查询任务状态
  getTaskStatus(taskId) {
    return request.get(`/task/${taskId}`)
  },
  
  // 获取比对结果
  getResult(taskId) {
    return request.get(`/result/${taskId}`)
  },
  
  // 删除任务
  deleteTask(taskId) {
    return request.delete(`/task/${taskId}`)
  },
  
  // 导出报告
  exportReport(taskId, formats = ['doc', 'html']) {
    return request.post('/export-report', { taskId, formats }, {
      responseType: 'blob'
    })
  }
}
```

### 4. 配置文件

**config.js**

```javascript
export const ZHAOXIN_CONFIG = {
  frontendUrl: 'http://localhost:3000',  // 肇新前端地址（iframe 嵌套用）
  apiBaseUrl: 'http://localhost:8080',   // 肇新后端地址
  demoBaseUrl: 'http://localhost:8091'   // Demo 后端地址
}
```

---

## 完整示例

### 比对流程

```javascript
// 1. 上传文件到肇新服务
const uploadFile = async (file) => {
  const formData = new FormData()
  formData.append('file', file)
  const response = await axios.post(
    `${ZHAOXIN_CONFIG.apiBaseUrl}/api/file/upload`,
    formData
  )
  return response.data.data.fileUrl
}

// 2. 提交比对任务
const submitCompare = async (oldFileUrl, newFileUrl) => {
  const response = await compareApi.submitCompare(
    oldFileUrl, 
    newFileUrl, 
    removeWatermark.value,
    oldFile.value.name,
    newFile.value.name
  )
  return response.data  // 返回 taskId
}

// 3. 轮询任务状态
const pollStatus = async (taskId) => {
  await pollTaskStatus(taskId, (progress, status) => {
    // 更新进度
    console.log(`进度: ${progress}%`)
    console.log(`状态: ${status.statusMessage}`)
  })
}

// 4. 获取结果
const getResult = async (taskId) => {
  const response = await compareApi.getResult(taskId)
  return response.data
}

// 5. 导出报告
const exportReport = async (taskId) => {
  const response = await compareApi.exportReport(taskId, ['doc', 'html'])
  
  // 创建下载链接
  const blob = new Blob([response.data], { type: 'application/zip' })
  const url = window.URL.createObjectURL(blob)
  const link = document.createElement('a')
  link.href = url
  link.download = `comparison-result-${taskId}.zip`
  link.click()
}
```

---

## 常见问题

### 1. 文件上传失败

**问题**：文件上传时报错 "文件大小超过限制"

**解决**：
- 检查文件是否超过 50MB
- 修改 `application.yml` 中的 `max-file-size` 配置

**问题**：文件上传时报错 "不支持的文件类型"

**解决**：
- 确保文件格式为 PDF
- 检查文件扩展名是否正确

### 2. 比对任务一直处于处理中

**问题**：任务状态长时间不更新

**解决**：
- 检查肇新后端服务是否正常
- 查看后端日志是否有错误信息
- 确认文件是否过大导致处理缓慢

### 3. iframe 结果页显示不全

**问题**：iframe 中显示了肇新前端的侧边栏和导航栏

**解决**：
- 确认 URL 中包含 `?embed=true` 参数
- 检查肇新前端是否支持嵌入模式

**问题**：iframe 页面无法加载

**解决**：
- 检查肇新前端服务是否正常运行
- 确认 `config.js` 中的 `frontendUrl` 配置正确
- 查看浏览器控制台是否有跨域错误

### 4. 下载报告失败

**问题**：点击下载按钮没有反应

**解决**：
- 检查浏览器是否阻止了下载
- 查看后端日志是否有错误
- 确认任务状态为已完成

---

## 最佳实践

### 1. 用户体验优化

- ✅ 提供文件上传进度提示
- ✅ 显示比对进度百分比
- ✅ 支持拖拽上传文件
- ✅ 提供清晰的错误提示

### 2. 性能优化

- ✅ 使用异步任务处理大文件
- ✅ 实现任务队列管理
- ✅ 添加任务缓存机制
- ✅ 优化轮询间隔

### 3. 错误处理

- ✅ 统一的错误响应格式
- ✅ 友好的错误提示信息
- ✅ 详细的日志记录
- ✅ 异常情况的降级处理

### 4. 安全性

- ✅ 文件类型验证
- ✅ 文件大小限制
- ✅ 请求超时控制
- ✅ 敏感信息脱敏

---

## 下一步

### 扩展功能建议

1. **批量比对** - 支持多个文件同时比对
2. **比对模板** - 保存常用的比对配置
3. **结果分享** - 生成分享链接
4. **历史记录** - 持久化任务历史

### 学习资源

- 📖 [智能文档抽取集成指南](./智能文档抽取集成指南.md)
- 📖 [合同合成集成指南](./合同合成集成指南.md)
- 💻 查看完整源码了解实现细节

---

## 技术支持

遇到问题？

1. 查看本文档的常见问题部分
2. 检查浏览器控制台和后端日志
3. 参考完整的 Demo 源码
4. 联系肇新技术支持团队

