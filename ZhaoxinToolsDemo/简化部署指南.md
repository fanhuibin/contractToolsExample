# 肇新工具集Demo - 简化部署指南

## 📦 核心文件

只需要这两个文件即可完成部署：

1. **`build.bat`** - 构建脚本
2. **`zhaoxin-demo-linux.zip`** - 部署包

## 🚀 快速部署

### 步骤1：本地构建

在Windows开发环境中：

```bash
# 执行构建脚本
echo 1 | .\build.bat
```

**构建过程**：
- ✅ 编译后端Java项目
- ✅ 构建前端Vue项目  
- ✅ 打包到 `dist` 目录
- ✅ 生成可执行的部署包

### 步骤2：服务器部署

将 `zhaoxin-demo-linux.zip` 上传到Linux服务器：

```bash
# 1. 上传文件到服务器
scp zhaoxin-demo-linux.zip user@server:/opt/

# 2. 解压部署包
cd /opt/
unzip zhaoxin-demo-linux.zip

# 3. 进入部署目录
cd zhaoxin-demo-linux/

# 4. 启动服务
chmod +x deploy-linux.sh
./deploy-linux.sh
```

### 步骤3：访问应用

部署完成后，通过浏览器访问：

```
http://服务器IP:81
```

## 📋 部署包内容

`zhaoxin-demo-linux.zip` 包含：

```
zhaoxin-demo-linux/
├── dist/                    # 应用文件
│   ├── backend/            # 后端JAR包
│   ├── frontend/           # 前端静态文件
│   ├── config/             # 配置文件
│   └── start.bat           # Windows启动脚本
├── deploy-linux.sh         # Linux部署脚本
└── README.md              # 部署说明
```

## ⚙️ 配置说明

### 端口配置

- **前端访问端口**：81
- **后端API端口**：8091
- **所有服务统一通过端口81访问**

### API配置

确保肇新API服务正常运行：
- **肇新SDK地址**：`http://192.168.0.10:8080`
- **比对API**：`/api/compare-pro/submit-url`

## 🔧 故障排除

### 常见问题

**1. 端口占用**
```bash
# 检查端口占用
netstat -tlnp | grep :81
netstat -tlnp | grep :8091

# 停止占用进程
sudo kill -9 <PID>
```

**2. 权限问题**
```bash
# 给予执行权限
chmod +x deploy-linux.sh
sudo chown -R $USER:$USER /opt/zhaoxin-demo-linux/
```

**3. Docker未安装**
```bash
# 安装Docker
curl -fsSL https://get.docker.com | sh
sudo systemctl start docker
sudo systemctl enable docker
```

### 日志查看

```bash
# 查看部署日志
tail -f /opt/zhaoxin-demo-linux/logs/deploy.log

# 查看应用日志（已优化，简洁模式）
docker logs zhaoxin-demo-backend
docker logs zhaoxin-demo-frontend

# 查看详细日志文件
tail -f /opt/zhaoxin-demo-linux/logs/application.log
```

### 日志优化特性

**生产环境日志特点**：
- ✅ **简洁格式** - 只显示关键信息
- ✅ **级别控制** - 只记录WARN及以上级别
- ✅ **错误过滤** - 502错误不再打印HTML内容
- ✅ **文件管理** - 自动轮转，保留15天历史

**日志示例**：
```
11-13 18:03:01 INFO  CompareController - 收到比对请求
11-13 18:03:01 ERROR GlobalExceptionHandler - 外部API调用失败: 502 - Bad Gateway
11-13 18:03:01 INFO  CompareController - 返回错误响应给用户
```

## 📊 系统要求

### 服务器要求

- **操作系统**：Linux (Ubuntu 18.04+, CentOS 7+)
- **内存**：≥ 2GB
- **磁盘空间**：≥ 5GB
- **Docker**：≥ 20.10
- **网络**：开放端口 81, 8091

### 开发环境要求

- **操作系统**：Windows 10+
- **Java**：JDK 11+
- **Node.js**：16+
- **Maven**：3.6+

## 🎯 功能验证

部署完成后，验证以下功能：

### 1. 基础功能
- ✅ 访问首页：`http://服务器IP:81`
- ✅ 文件上传功能
- ✅ 各模块页面正常显示

### 2. 核心功能
- ✅ **智能文档比对** - 上传PDF进行比对
- ✅ **智能文档抽取** - 提取文档信息
- ✅ **合同合成** - 生成合同文档
- ✅ **模板管理** - 管理文档模板

### 3. API连通性
```bash
# 测试后端API
curl http://服务器IP:8091/api/system/info

# 测试肇新API连通性
curl http://192.168.0.10:8080/api/compare-pro/submit-url
```

## 🔄 更新部署

如需更新应用：

```bash
# 1. 重新构建
echo 1 | .\build.bat

# 2. 重新部署
scp zhaoxin-demo-linux.zip user@server:/opt/
cd /opt/
./deploy-linux.sh
```

## 📞 技术支持

如遇问题，请检查：

1. **网络连通性** - 确保服务器网络正常
2. **端口开放** - 确保防火墙开放相应端口
3. **服务状态** - 确保Docker服务正常运行
4. **日志信息** - 查看详细错误日志

---

## 🎉 总结

通过这两个核心文件，可以实现：

- **一键构建** - `build.bat` 完成所有构建工作
- **一键部署** - `zhaoxin-demo-linux.zip` 包含完整部署环境
- **零配置** - 预配置好所有必要设置
- **快速启动** - 几分钟内完成部署

简单、高效、可靠的部署方案！🚀
